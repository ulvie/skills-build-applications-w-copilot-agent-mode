# OctoFit Tracker

Ports used by this multi-tier app:

- Frontend (Vite + React): 5173
- Backend (Express): 8000
- MongoDB: 27017

Run `npm install` in each of `frontend` and `backend`, then use `npm run dev`.
